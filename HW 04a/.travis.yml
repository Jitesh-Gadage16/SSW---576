language: python
python:
  - "3.8"  # Adjust based on your Python version

install:
  - pip install -r requirements.txt  # Install dependencies

script:
  - python -m unittest discover tests  # Run unit tests
